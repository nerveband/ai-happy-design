# AI Happy Design Site

Astro + Starlight source for `aihappydesign.com`, managed inside the main `ai-happy-design` monorepo.

```bash
cd site
npm ci
npm run build
```

The root `netlify.toml` builds this folder and publishes `site/dist`.
